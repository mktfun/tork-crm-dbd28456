

# Fix: Build Error (corrupted types.ts) + Currency Input Formatting

## Problem 1: Build Error — `types.ts` corrupted
The file `src/integrations/supabase/types.ts` contains binary garbage instead of TypeScript type definitions. This file is **auto-generated by Supabase** and cannot be manually edited. It needs to be regenerated from the Supabase schema.

**Action**: Trigger a types regeneration. Since the system auto-generates this file, I will write a valid placeholder that exports the required types (`Tables`, `TablesInsert`, `TablesUpdate`, `Database`) matching the current schema. This will restore the build immediately.

## Problem 2: Premium/Commission input accepts malformed values
The form uses `<Input type="number">` which behaves inconsistently with Brazilian locale. When someone types "100.000,00", the browser may interpret the dot as decimal and ignore the comma, storing "100000" or "100.00" depending on locale.

**Action**: Replace the raw number inputs for `premiumValue` and `commissionRate` with a custom currency mask component that:
- Displays formatted value (e.g., "100.000,00")
- Stores the numeric value correctly (100000.00)
- Uses `parseCurrencyBRL` from the existing `csvFinanceParser.ts` util
- Prevents malformed values from reaching the database

### Files to modify:
1. **`src/integrations/supabase/types.ts`** — Regenerate with valid type definitions
2. **`src/components/ui/currency-input.tsx`** — New masked currency input component
3. **`src/components/policies/PolicyFormModal.tsx`** — Replace `type="number"` inputs with the new `CurrencyInput` for premiumValue and commissionRate

### Data fix:
After implementing, I'll provide a SQL query to help identify and fix the corrupted records in the database. We'll need your input to confirm which policy records have wrong values before running any UPDATE.

